!!!
%html
	%head
	%body
		= partial :"partials/header"
		-if flash[:errors] && !flash[:errors].empty?
			%ul{class: 'errors'}
				-flash[:errors].each do |error|
					%li
						=error
		-if flash[:notice]
			%div{ id: 'notice' }
				=flash[:notice]
		=yield
